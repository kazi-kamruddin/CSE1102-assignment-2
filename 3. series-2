#include<stdio.h>

int main()
{
    long int n,i,j,add=0,sum=0;
    printf ("enter a number: ");
    scanf("%ld", &n);

    for(i=1;i<=n;i++)
    {
        add += i;
        sum += add;
    }
    printf ("\n1+(1+2)+(1+2+3)+...+(1+2+3+...+%ld)= %ld\n", n, sum);
    return 0;
}
